# アートアセット仕様（本番イラストの用意と組み込み）

コンセプト画像（リポジトリ直下の `ChatGPT Image 2026年6月19日 *.png` 5枚）の世界観を、
アプリ内に**実イラストとして**反映するための仕様。オーナーが個別画像を生成 → 開発が
`Image.asset` で組み込む、という分担で進める。

> 現状(2026-06-30 更新): **卵 common/rare/sr/ssr の本番イラストを導入済み**。
> 参照シート（透過版 `94e6fbdb-….png` / `0f2a3be5-….png`）から、卵を**藁の巣ごと**切り出して
> `assets/images/egg/egg_{common,rare,sr,ssr}.png`（512×512 透過）にした。`EggArt` は
> `Image.asset` で表示し、**欠落・読み込み失敗時のみ**暫定ベクター（`_EggPainter`）に
> フォールバックする。差し替え点は卵=1ウィジェット（`EggArt`）に集約済みで、ホーム主役・
> オンボ・歓迎画面・たまご/保管/育成枠（`EggSubject` 経由）まで全てこのウィジェットを通る。
>
> **【デザイン決定 2026-06-30 / product-designer】卵は「藁の巣ごと」採用**:
> 当初仕様は「卵画像に巣を含めない（巣はアプリの砂リングが描く）」だったが、参照シートの卵は
> 全て藁の巣に乗っており、卵だけを抜くと **rare(青)で藁残り/底潰れ・解像度ロス**が出た。
> デザイナー判断（3案 A巣なし / B巣あり×砂リング / C巣あり×リング無し を実物比較）で **B を採用**：
> - **「藁の巣＝卵のおうち（中身）」と「砂リング＝全画面共通の台座（額縁）」の二層**として読め、
>   "二重の巣" には見えない。署名要素「砂リング」（`nest_panel.dart`）は **無改修で維持**。
> - 全レアリティが **無加工で最も綺麗**（マスク不要＝rare の不具合解消・解像度ロス無し・制作も最小）。
> - 被写体ごとに中身を出し分け: 卵=巣付き / Mofi=巣なし（動物は巣から出て自然）/ 空巣状態=空の藁の巣。
>
> **残る限界・将来対応**:
> - **成長ヒビの段階別アセットは無い**（同一卵のヒビ違いが素材に無い）。ヒビ段階は進捗バー＋
>   「孵化まであと Xpt」で表現し、画像自体は段階で変えない（フォールバックのベクターのみ `progress` 描画）。
> - 解像度は元 ~110px の卵を 512 に拡大。やわらか3D風で鑑賞距離は許容だが、くっきり本番品質に
>   上げるなら §2 のとおり各卵を 1024 で個別生成して差し替える。
> - 図鑑の極小サムネ（〜40px）は藁が潰れる懸念 → 実機確認し、潰れたらサムネだけ padding 調整。
> - **空巣状態の「空の藁の巣」専用アセット**は未用意（現状は `Icons.egg_outlined` プレースホルダ）。
> - PNG 容量は 4枚で約 0.7MB（可逆 optimize 済）。mofi/ui 追加時に pngquant/oxipng で一括圧縮を。

---

## 1. 共通仕様（全アセット）

- **形式**: PNG、**背景透過**（必須）。余白は被写体の周囲に均等に少しだけ。
- **解像度**: 正方形キャンバス。卵・キャラは **1024×1024 px**（高解像度1枚でOK。Flutter が縮小）。
  - 端末解像度別（@2x/@3x）を分けなくてよい（単一高解像度を `Image.asset` で表示）。
- **トーン**: コンセプト準拠＝やわらかい3D風・パステル・あたたかい。影は黒でなく茶系。
- **命名**: 半角英小文字・スネークケース。下記の正確な名前で（コードが参照する）。
- **配置先**: 後述の `assets/images/...` フォルダにそのまま入れる。

---

## 2. 優先度1：卵（最優先・オーナーの最重要指摘）

> 現状は **レアリティごとに1枚のベース画像**（`egg_common/rare/sr/ssr.png`）で運用中
> （参照シートからの切り出し）。下表の **段階別12枚**は「くっきりした本番品質に上げる」
> ための将来仕様（同一卵のヒビ違いを個別生成して差し替えると、画像でヒビ進行も出せる）。

卵は **レアリティ4種 × 成長3段階 = 12枚**。レアリティ色はデザイントークンに準拠：
common=灰緑 / rare=青 / sr=紫 / ssr=金。

| レアリティ | 段階 | ファイル名 | 備考 |
|---|---|---|---|
| common | 無傷 | `egg_common_0.png` | 孵化進捗 0〜19% |
| common | ヒビ小 | `egg_common_1.png` | 20〜49% |
| common | ヒビ大 | `egg_common_2.png` | 50〜99%（孵化直前） |
| rare | 無傷/小/大 | `egg_rare_0.png` / `_1` / `_2` | 同上 |
| sr | 無傷/小/大 | `egg_sr_0.png` / `_1` / `_2` | 同上 |
| ssr | 無傷/小/大 | `egg_ssr_0.png` / `_1` / `_2` | きらめき入りでも可 |

- **巣は含めない**（巣はアプリ側の「巣リング」が描く）。卵単体を中央に、底が少し平らだと収まりが良い。
- まず **common の3枚**だけでも先に入れれば、暫定描画から実イラストに切り替えられる。

配置先: `assets/images/egg/`

---

## 3. 優先度2：Mofi（キャラ・図鑑/孵化で使用）

MVPは **15種 × 2（通常・色違い）= 30枚**（PRD §5 図鑑30）。3系統：
- スライム系（例: ぷにモフィ/ぷるモフィ/キングスライム/エンペラスライム）
- どうぶつ系（例: モフハム/フェネモフィ/フワリス/モフキング）
- ドラゴン系（例: ベビードラ/ホカドラ/フレイムドラ/エンシェントラン）

| 種別 | ファイル名 | 備考 |
|---|---|---|
| 通常 | `mofi_{species}.png` | 例: `mofi_punimofi.png` |
| 色違い | `mofi_{species}_shiny.png` | S13 色違い（配色違い） |

- 正面・全身・かわいいポーズ。図鑑サムネ＆孵化演出で使う。
- 種スラッグ（`{species}`）の確定リストは別途すり合わせ（コードの図鑑エントリ定義と一致させる）。

配置先: `assets/images/mofi/`

---

## 4. 優先度3：UI/通貨/バッジ（任意・後回し可）

| 用途 | ファイル名 | 備考 |
|---|---|---|
| ポイント | `ui_point.png` | 通貨バッジ用（無くてもアイコンで代替中） |
| ジェム | `ui_gem.png` | 〃 |
| レアR/SR/SSR | `badge_r.png` / `badge_sr.png` / `badge_ssr.png` | 現状はコードのバッジで代替可 |

配置先: `assets/images/ui/`

---

## 5. 組み込み手順（開発側・アセットが届いたら）

1. 上記フォルダに画像を入れる。
2. `pubspec.yaml` の `flutter:` に次を追加（**ファイルが存在してから**追加する／空ディレクトリ宣言は `flutter pub get` が失敗する）:
   ```yaml
   flutter:
     uses-material-design: true
     assets:
       - assets/images/egg/
       - assets/images/mofi/
       - assets/images/ui/
   ```
3. `EggArt`（`lib/core/widgets/egg_art.dart`）を `Image.asset('assets/images/egg/egg_${rarity}_${stage}.png')` ベースに置換（暫定描画はフォールバックとして残せる）。
4. 図鑑・孵化演出の Mofi も `Image.asset('assets/images/mofi/...')` に。
5. iOS は **配信前に同梱必須**（google_fonts はネット取得だが画像は同梱が安全）。CI で `flutter analyze`／実機で表示確認。

---

## 6. まず最初の一歩（最小で効果を出す）→ ✅ 卵は導入済み

✅ **完了(2026-06-30)**: 参照シートから卵を切り出し、ホーム主役・歓迎画面・たまご/保管/
育成枠まで実イラストに切替済み（暫定ベクターからアップグレード）。次の一手は精度向上か
キャラ（Mofi）への展開：

- **(任意) 卵をくっきり本番品質に**: 各卵を §1 準拠（1024・透過・**藁の巣ごと**＝B案）で
  個別生成し、`assets/images/egg/egg_{rarity}.png` を置き換え（解像度向上）。段階別（`_0/_1/_2`）
  まで作れば画像でヒビ進行も出せる（`EggArt` 側の対応は別途）。
- **次の優先**: §3 の Mofi（図鑑・孵化演出）。`mofi_{species}.png` を揃え、`assets/images/mofi/`
  を pubspec に追加して `MofiSubject`（`egg_visuals.dart`）を `Image.asset` 化する。
